rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Cognaxis routes confidential data through its authenticated backend.
    // The Admin SDK bypasses these rules, so API authorization remains mandatory.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
